#include <iostream>

using namespace std;

int SyntaxPresent(int num)
{
	// if the total of characters is bigger than 0 it will return true

	if (num > 0)
	{
		return true;
	} else
	{
		return false;
	}
}
